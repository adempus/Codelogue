query userFolders {
    getUserFolders {
        id
        name
        snippets {
            edges {
                node {
                    id
                    title
                }
            }
        }
    }
}
