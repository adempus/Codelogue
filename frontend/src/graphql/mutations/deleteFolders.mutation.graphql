mutation deleteFolders($folderIds: [String!]!) {
    deleteFolders(folderIds: $folderIds) {
        folders {
            id
            name
        }
        status {
            error
            message
        }
    }
}
