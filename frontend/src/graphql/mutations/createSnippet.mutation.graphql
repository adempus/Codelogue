mutation createSnippet($input: CreateSnippetInput!) {
    createSnippet(input: $input) {
        status {
            error
            message
        }
        snippet {
            id
        }
    }
}
