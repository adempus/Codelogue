mutation createTaggedSnippet($snippetId: ID!, $tagIds: [ID!]!){
    createTaggedSnippet (snippetId: $snippetId, tagIds: $tagIds) {
        taggedSnippet {
            snippetId
            tagId
        }
    }
}
